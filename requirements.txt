# Python 3.12 — versions verified together in .venv (2026-09-13).
# Install into a virtualenv, NOT the global Python:
#   python -m venv .venv
#   .venv\Scripts\python.exe -m pip install -r requirements.txt

# Web server
fastapi==0.141.1
starlette==1.6.0
uvicorn==0.52.4
pydantic==2.13.5

# Data
pandas==3.0.5
openpyxl==3.1.5
numpy==2.5.3

# Embeddings + vector store
torch==2.14.0
transformers==5.17.0
tokenizers==0.23.2
huggingface_hub==1.31.0
sentence-transformers==6.0.1
chromadb==1.5.9

# LLM + web search
ollama==0.6.2
duckduckgo-search==8.1.1

# Truy hồi từ khoá (BM25 bỏ dấu) + tokenizer cho reranker
rank_bm25==0.2.2
sentencepiece==0.2.2
protobuf==7.36.1

# Xác thực (băm mật khẩu)
bcrypt==5.0.0
